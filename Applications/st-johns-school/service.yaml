apiVersion: v1
kind: Service
metadata:
  name: school-website
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: school-website
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP